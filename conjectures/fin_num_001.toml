id = "fin_num_001"
title = "Interval bounds on discounted call intrinsic value"
difficulty = "hard"
prover = "rocq"
status = "open"
preamble = "Require Import Reals.\nRequire Import Lra.\nOpen Scope R_scope.\n(* Requires Flocq and CoqInterval for full proof *)"
lemma_statement = "(* Simplified version: verify that a specific discounted intrinsic value is positive.\n   The full version uses Flocq for IEEE 754 modeling and CoqInterval's interval tactic. *)\nLemma call_price_bound : forall S K r T sigma : R,\n  S = 100 -> K = 100 -> r = 5/100 -> T = 1 -> sigma = 20/100 ->\n  let price := S - K / (1 + r) in\n  0 < price."
skeleton = "Lemma call_price_bound : forall S K r T sigma : R,\n  S = 100 -> K = 100 -> r = 5/100 -> T = 1 -> sigma = 20/100 ->\n  let price := S - K / (1 + r) in\n  0 < price.\nProof.\n  (* your proof here *)\nAdmitted."
hints = [
    "Substitute the concrete values",
    "Simplify using field_simplify or lra",
    "The full version would use interval tactic from CoqInterval",
]

[dependencies]
rocq_version = "9.0"
opam_packages = [
    "flocq",
    "coq-interval",
]
