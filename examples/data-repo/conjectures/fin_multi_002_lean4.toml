id = "fin_multi_002_lean4"
title = "Self-financing portfolio wealth identity"
difficulty = "medium"
prover = "lean4"
status = "open"
preamble = "import Mathlib.Tactic"
lemma_statement = "theorem self_financing (Δ_old Δ_new B_old B_new S r : ℝ)\n    (h_sf : Δ_new * S + B_new * (1 + r) = Δ_old * S + B_old * (1 + r)) :\n    (Δ_new - Δ_old) * S = (B_old - B_new) * (1 + r) := by"
skeleton = "theorem self_financing (Δ_old Δ_new B_old B_new S r : ℝ)\n    (h_sf : Δ_new * S + B_new * (1 + r) = Δ_old * S + B_old * (1 + r)) :\n    (Δ_new - Δ_old) * S = (B_old - B_new) * (1 + r) := by\n  sorry"
hints = [
    "Rearrange the self-financing condition",
    "linarith closes this",
]

[dependencies]
lean_toolchain = "leanprover/lean4:v4.16.0"
lake_packages = [
    "mathlib",
]
