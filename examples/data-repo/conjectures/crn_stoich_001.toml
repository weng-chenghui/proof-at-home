id = "crn_stoich_001"
title = "H-atom conservation for water formation"
difficulty = "easy"
prover = "lean4"
status = "open"
preamble = "import Mathlib.Data.Matrix.Basic\nimport Mathlib.Data.Fin.VecNotation\nimport Mathlib.Tactic"
lemma_statement = "theorem h_atom_conservation :\n    let S : Matrix (Fin 3) (Fin 1) ℤ := !![(-2); (-1); (2)]\n    let aH : Fin 3 → ℤ := ![2, 0, 2]\n    ∀ j : Fin 1, ∑ i : Fin 3, aH i * S i j = 0 := by"
skeleton = "theorem h_atom_conservation :\n    let S : Matrix (Fin 3) (Fin 1) ℤ := !![(-2); (-1); (2)]\n    let aH : Fin 3 → ℤ := ![2, 0, 2]\n    ∀ j : Fin 1, ∑ i : Fin 3, aH i * S i j = 0 := by\n  sorry"
hints = [
    "Introduce j and case-split on the single index with fin_cases",
    "After fin_cases, simp and norm_num should close the goal",
    "Alternatively, decide may work directly on the decidable equality",
]

[dependencies]
lean_toolchain = "leanprover/lean4:v4.16.0"
lake_packages = [
    "mathlib",
]
